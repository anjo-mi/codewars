<h2><a href=https://www.codewars.com/kata/567c9f56d83baeed8300000f/train/javascript target="_blank">Regexp Basics - is it a hexadecimal number?</a></h2><h3>7 kyu</h3><p>Implement <code>String#hex_number?</code> (in Java <code>StringUtils.isHexNumber(String)</code>), which should return <code>true</code> if given object is a hexadecimal number, <code>false</code> otherwise.</p><p>Hexadecimal numbers consist of one or more digits from range <code>0-9</code> <code>A-F</code> (in any case), optionally prefixed by <code>0x</code>.</p>