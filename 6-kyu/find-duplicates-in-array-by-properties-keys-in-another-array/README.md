<h2><a href=https://www.codewars.com/kata/58cc070abd22e324b300002a/train/typescript target="_blank">Find duplicates in array by properties keys in another array</a></h2><h3>6 kyu</h3><p>Your task is to return duplicated objects from an array.</p><p>You will receive two arrays, <code>objs</code> and <code>keys</code>. Duplicate objects mean that the object properties defined on keys are duplicated, for example:</p><pre><code>// keys[ 'x', 'y' ]// objs{ x: 1, y: 1 },{ x: 2, y: 2 },{ x: 1, z: 1 },{ x: 1, y: 1, z: 3 },</code></pre><p>The expected result is:</p><pre><code>{ x: 1, y: 1 },{ x: 1, y: 1, z: 3 },</code></pre><p>Because <code>x</code> and <code>y</code> repeat on the first and last element, so they will be picked up.</p><p>If a key is not present in the object the value of this property will be considered "not defined" native value for the current language.</p><p>Similar:</p><ul><li><a href="https://www.codewars.com/kata/find-unique-in-array-by-properties-keys-in-another-array" data-turbolinks="false" target="_blank">Find unique in array by properties keys in another array</a></li></ul>