<h2><a href=https://www.codewars.com/kata/59290e641a640c53d000002c/train/typescript target="_blank">#1 Sequences: Pure Even Digit Perfect Squares (P.E.D.P.S)</a></h2><h3>6 kyu</h3><p>The integer <code>64</code> is the first integer that has all of its digits even and furthermore, is a perfect square.</p><p>The second one is <code>400</code> and the third one <code>484</code>.</p><p>Give the numbers of this sequence that are in the range <code>[a,b]</code> (both values inclusive)</p><p>Examples:</p><pre><code>Even digit squares between 100 to 1000:  [400, 484]  (the output should be sorted)Even digit squares between 1000 to 4000: []</code></pre><p>Features of the random tests:</p><pre><code>number of tests = 167maximum value for a = 1e10maximum value for b = 1e12</code></pre><p>You do not have to check the entries; <code>a</code> and <code>b</code> are always positive integers and <code>a &lt; b</code>.</p><p>Happy coding!!</p>