<h2><a href=https://www.codewars.com/kata/59b139d69c56e8939700009d/train/typescript target="_blank">Largest integer exponent</a></h2><h3>6 kyu</h3><p>Write a method named <code>getExponent(n,p)</code> that returns the largest integer exponent <code>x</code> such that p<sup>x</sup> evenly divides <code>n</code>. if <code>p&lt;=1</code> the method should return <code>null</code>/<code>None</code> (throw an <code>ArgumentOutOfRange</code> exception in C#).</p>