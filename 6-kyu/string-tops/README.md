<h2><a href=https://www.codewars.com/kata/59b7571bbf10a48c75000070/train/javascript target="_blank">String tops</a></h2><h3>6 kyu</h3><h1 id="task">Task</h1><p>Write a function that accepts <code>msg</code> string and returns local tops of string from the highest to the lowest.<br>The string's tops are from displaying the string in the below way:</p><pre><code>                                                      3                              p                     2   4            g               o   q                 1  b       f   h           n       r             za   c   e       i       m          s          y      d           j   l             t       x                    k                 u   w                                        v</code></pre><p>The next top is always 1 character higher than the previous one. For the above example, the solution for the <code>abcdefghijklmnopqrstuvwxyz1234</code> input string is <code>3pgb</code>.</p><ul><li>When the <code>msg</code> string is empty, return an empty string.</li><li>The input strings may be very long. Make sure your solution has good performance.</li></ul><p>Check the test cases for more samples.</p>